name: Cypress Tests

on: push

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Cypress run
        uses: cypress-io/github-action@v4.2.0
        with:
          spec: cypress/e2e/1-getting-started/todo.cy.js
          # pass the Dashboard record key as an environment variable
          CYPRESS_RECORD_KEY: c2796174-71fb-4604-b6c8-072da040da6b
          # pass GitHub token to allow accurately detecting a build vs a re-run build
          #GITHUB_TOKEN: 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
          #  build: npm run build
          #  start: npm start